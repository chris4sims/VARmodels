fliproots <- function(A0,A1) {
  ## Returns an n x n matrices B0, B1 such that all the roots of |B0-B1z| are on
  ## or outside the unit circle,
  ## (B0-B1z)(B0'-B1'conj(z) = (A0-A1z)(A0'-A1'conj(z)).
  ## (This is empty, because not needed for the immediate (7/09) project.)
}
